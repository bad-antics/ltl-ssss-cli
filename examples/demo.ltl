// examples/demo.ltl — runnable demo for ltl_ssss_cli

import std::io
use ltl_ssss_cli as lib

fn main() {
    io::println("ltl_ssss_cli demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}